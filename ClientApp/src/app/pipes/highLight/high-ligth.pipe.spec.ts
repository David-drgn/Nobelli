import { HighLigthPipe } from './high-ligth.pipe';

describe('HighLigthPipe', () => {
  it('create an instance', () => {
    const pipe = new HighLigthPipe();
    expect(pipe).toBeTruthy();
  });
});
